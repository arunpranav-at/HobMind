# Example environment variables for the Next.js frontend
# This file is safe to commit. Copy to `.env.local` and fill in real values for local development.

# Base URL for the backend API used by the frontend. Replace with your backend URL.
# Example: NEXT_PUBLIC_API_BASE='http://localhost:5000'
NEXT_PUBLIC_API_BASE=http://localhost:5000

# Add other public environment variables here. Variables exposed to the browser
# must start with NEXT_PUBLIC_. Keep secrets out of frontend env files.
